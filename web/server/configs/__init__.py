"""Read-only viewer for the YAML configs that ship with agentforge.

Mounted under ``/api/configs``. Useful for verifying that a deploy carried
the expected file content into the running container.
"""
