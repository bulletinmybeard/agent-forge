"""Drop private tool-plugin modules here (gitignored).

Each exposes a register(registry) -> int. Load via AGENTFORGE_TOOL_PLUGINS,
e.g., AGENTFORGE_TOOL_PLUGINS=plugins.cloud_tools:register_cloud_tools
"""
